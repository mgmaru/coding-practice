package algo

// 06. quickselect（k番目に小さい値） — README.md「Tier 2 / 6. quickselect」参照
// 全ソートせず、小さい方から k 番目（k=1 が最小, 1-indexed）を平均 O(n) で求める。
// partition を書き、k がどちら側かで再帰する側を 1 つに絞る（クイックソートと違い片側だけ）。
// TODO: 自分で実装する
