package algo

// 07. スタック／キュー — README.md「Tier 3 / 7. スタック／キュー」参照
// slice で stack(LIFO)/queue(FIFO) を push/pop/peek/空判定 O(1)（償却）で。
// 動作確認: スタックで括弧の対応チェックを書く。
// 罠: キューを q=q[1:] で先頭 pop すると O(n)／メモリが伸びる。head index 等の選択肢を notes に。
// TODO: 自分で実装する
