package algo

// 09. ハッシュ表 のテスト（操作列で検証）
// Put("a",1),Put("b",2),Put("a",3) の後:
//   Get("a") → (3,true)（上書き）, Get("b") → (2,true), Get("z") → (0,false)
// さらに Delete("a") の後: Get("a") → (0,false)
// 境界:
//   - 存在しないキーの Get → (0,false)（collections #7 の v,ok と地続き）
//   - 同じキーの上書き（要素数は増えない）/ 削除後の再取得
//   - 衝突する 2 キーで両方引ける
//   - リサイズをまたいでも全キーが引ける
// TODO: テストを書く
