package algo

// 12. トポロジカルソート のテスト
// 入力例(DAG): 有向辺 A→B, A→C, B→D, C→D → 制約を満たす順序の一例 [A,B,C,D]
// 閉路の例: A→B, B→C, C→A → 「閉路あり（順序づけ不能）」
// 境界:
//   - 閉路の検出
//   - 孤立頂点（辺を持たない頂点も出力に含める）
//   - 妥当な順序が複数（一意でない）→ 完全一致で検証しない。
//     「全ての辺 u→v で u が v より前にあるか」で検証する（collections #4 と同じ発想）。
// TODO: テストを書く
