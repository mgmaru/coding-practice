# 反省

## ドキュメント概要
日々やったことや気づき、学ぶべきこと、反省をメモする。

## 反省
## Phase 0
### 2026-06-07
#### やったこと
- リポジトリのルール整備
    - AIのコードレビューのレビュー観点整備
    - PR作成ワークフロー整備
- Python環境構築
- Skill設計
#### 気づき
- Skill設計
    - (汎用)ドキュメント作成：ドキュメントの書き方を教える。
        - ルールなどはドキュメントの最初に記載（常に参照できるように）
        - (1)、①などの数字の禁止
    - (汎用)コードレビュー：レビュー観点を教える。
    - (プロジェクト)フェーズ移行：フェーズ移行の承認プロセス、基準を教える。
    - (プロジェクト) チケット作成：チケットの書き方を教える。
        - 課題を見て、どのような機能を作るのかを決定する。
        - 受け入れ条件を作成する。
    - (プロジェクト) 課題作成：新しい課題の提案方法を教える。
        - 現在のリポジトリを確認して、挑戦していない領域を洗い出して、新しい課題を提案する。
- Python環境構築
    - uvを使用してプロジェクト初期化
---------------
---------------
## Phase 1
### 2026-06-08
#### やったこと
- 多言語環境構築
#### 気づき
- Nodeのパッケージマネージャーについて、今まで使用していた`npm`ではなく、`pnpm`を使用することにした。
    - その中でも、`corepack`を使用して管理することで、`package.json`に`packageManager`というフィールドを設定するだけで、`pnpm`のバージョンを固定できるので、便利だと思った。
    - ただし、`packageManager`では、`pnpm`のバージョンを指定する必要がある。`^`などの範囲指定は不可。
---------------
### 2026-06-09
#### やったこと
- 言語別に(format/lint/型)チェックのGitHub Actionsを追加
- コードレビュー用のSkillを作成
#### 気づき
- 言語別に(format/lint/型)チェックのGitHub Actionsを追加
    - 言語別にディレクトリを作成しているので、管理しやすいように言語別にCIファイルを分けた。
    - PythonのCIファイル
        - `ruff`（Linter）と`myoy`(型チェック)が、pyproject.tomlの依存関係に入っていない場合、`uvx`で簡易的に実行するようにした（ただし、再現性を出したいのであれば、`uv`の方が良いかも）。
    - CIファイルの中の`python-version`や、`ruff`、`mypy`のバージョンを固定した方が良い。
    - 依存関係が増えた時は、`uv sync --locked`にした方が良さそう。
- CIファイル変更時の構文チェックのために、`actionlint`を導入した。
---------------
### 2026-06-10〜2026-6-11
#### やったこと
- `go/phase1/tools/logstat`の実装&レビュー
#### 気づき
- 実装に時間かけすぎた。。。
- 基本的なデータ構造（スライス、map、JSONなど）の操作方法が、まだおぼつかない気がした。
    - これはかなり致命的なので、CLIツールを実装して慣れていきたい。
- 最初に書いたコードをAIにレビュー（`/rubic-review`）してもらったあとは、AIに`Issue`を作成してもらった方が良い（チェックボックス入り）。
    - 修正後は`Issue`を参照して、修正されているかAIに確認してもらう。
---------------
### 2026-06-12
#### やったこと
- `go/phase1/tools/logstat`の修正&レビュー
#### 気づき
- `git merge --no-ff <branch name>`を知る。
    - `--no-ff`をつけることで、マージした時にブランチからマージした記録が残るので、良いと思った。
    - ただ、マージしただけだと、`main`から見て、どこのブランチで作業をしてマージしたのかがわかりづらい。
    - 修正作業終了時のマージの時は、`--no-ff`オプションをつけた方が良いと思った。
- 修正作業は最初にブランチを切って、そこで修正作業は完結させた方が良いと思った。
    - 修正作業中に`main`に`checkuot`して作業しても良いが、ちょっと切り替えが面倒くさいので、修正作業中はブランチの中で作業した方が良い。
    - ただし、修正作業ではなく、プロジェクトを横断する作業（日々の反省の記録,`notes.md`の記載）などは、`main`に戻って作業するのも良いと思った。
---------------